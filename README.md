To run this visualizer, run:
    streamlit run VisualizerApp.py
on the command line.

Then visit the URL that will be shown on the command line.
    For example, you may see something like:
        "http://"
